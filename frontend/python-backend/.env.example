# Anthropic API Configuration
# Get your API key from https://console.anthropic.com/
ANTHROPIC_API_KEY=your_api_key_here

# If no API key is provided, the system will fall back to regex-based pattern matching
